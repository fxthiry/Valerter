//! Valerter - Real-time alerting from VictoriaLogs to Mattermost.

use anyhow::Result;

// Import the library to ensure it's compiled and linked
use valerter as _;

fn main() -> Result<()> {
    println!("valerter starting...");
    Ok(())
}
