# Valid config, but rules.d/ contains invalid YAML

victorialogs:
  url: http://victorialogs:9428

defaults:
  throttle:
    count: 5
    window: 1m
  notify:
    template: default_alert

templates:
  default_alert:
    title: "Alert"
    body: "{{ _msg }}"

rules: []
