# This rule has the same name as one in config.yaml -> collision!
collision_rule:
  query: "_msg:warning"
  parser:
    regex: ".*"
  notify:
    template: "default_alert"
    destinations:
      - "test"
